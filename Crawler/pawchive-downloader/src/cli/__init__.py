"""Interactive command layer.

    registry.py   command/param declarations, parsing, validation
    commands.py   handlers (hot-reloaded by path)
    shell.py      the prompt loop, dispatch and error policy
    prompt.py     prompt session, completers, status bar
"""
